var
prompt (esto es para ingresar datos)

javascript permise number, string, bool, char, etc.

el prompt siempre devuelve un string.



buscar windows.onLoad y defer


utilizar let para las variables.
const para las constantes

innerHtml buscar sobre este comando

let numeroA = document.getElementById("btn1").value --> esto para obtener el botón



e.preventDefault() esto se usa en el caso de que se use un formulario (form) y no recargue la pagina al apretar algo


agregar resultado en un html-->
let etiqueta = document.createElement('p');
etiqueta.appendChild(document.createTextNode()); //Esto me crea una linea de texto